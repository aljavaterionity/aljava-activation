/* ALJAVA TERIONITY — Sales Performance workspace */
(()=>{'use strict';const C=window.ALJAVA_CORE,sb=C?.supabase,$=C?.$,esc=C?.esc,money=C?.money;if(!sb||!$)return;let ready=false,busy=false,creating=false,detailBusy=false,sales=[];const S={start:null,end:null,search:'',status:'all'};const args=()=>({p_start:S.start?`${S.start}T00:00:00`:null,p_end:S.end?`${S.end}T23:59:59.999`:null});const fmtDate=v=>v?new Date(v).toLocaleDateString('id-ID'):'-';const msg=e=>{const m=String(e?.message||e||'').toLowerCase();if(/unauthorized|forbidden|row-level security/.test(m))return'Anda tidak memiliki izin untuk melakukan tindakan ini.';if(m.includes('name_required'))return'Nama sales wajib diisi.';if(m.includes('sales_code_required'))return'ID Sales wajib diisi.';if(m.includes('invalid_sales_code'))return'ID Sales hanya boleh berisi huruf, angka, garis bawah, atau tanda hubung.';if(m.includes('sales_code_exists'))return'ID Sales sudah digunakan.';if(m.includes('admin_business_unit_required'))return'Business unit admin tidak ditemukan.';if(m.includes('card_not_found'))return'Kode kartu tidak ditemukan pada business unit ini.';if(m.includes('card_already_assigned'))return'Kartu tersebut sudah dipegang sales lain.';if(m.includes('sales_inactive'))return'Sales tidak aktif.';return'Terjadi kesalahan. Silakan coba lagi.'};
function install(){if(ready)return;const app=$('app');if(!app)return;const l=document.createElement('link');l.rel='stylesheet';l.id='aljavaSalesWorkspaceCss';l.href='/assets/sales-workspace.css?v=20260907-0600';document.head.appendChild(l);const v=document.createElement('section');v.id='salesWorkspaceView';v.className='view';v.innerHTML=`<div class="row" style="margin-top:18px;gap:12px"><div><h1 style="margin:0">Sales Workspace</h1><p class="muted">Pantau performa staff sales, kartu yang dipegang, aktivasi, dan komisi.</p></div><div class="sales-workspace-toolbar"><button id="salesWorkspaceAdd" class="btn" type="button">+ Add Sales</button><button id="salesWorkspaceRefresh" class="btn" type="button">Refresh</button></div></div><section class="sales-workspace-panel"><div class="head"><div class="row sales-performance-head"><div><h2 style="margin:0">Sales Performance</h2><p class="muted" style="margin:4px 0 0">Nama, status, ID Sales, tanggal bergabung, closing, dan komisi.</p></div><div class="sales-workspace-toolbar sales-performance-filters"><input id="swSearch" class="field" placeholder="Cari nama / ID sales"><select id="swStatus" class="field"><option value="all">Semua status</option><option value="active">Active</option><option value="inactive">Inactive</option></select></div></div><div class="sales-period"><button class="btn period-btn" data-period="today" type="button">Today</button><button class="btn period-btn" data-period="7" type="button">7 Days</button><button class="btn period-btn" data-period="30" type="button">30 Days</button><button class="btn period-btn" data-period="month" type="button">This Month</button><button class="btn period-btn active" data-period="all" type="button">All Time</button><input id="swStart" class="field" type="date"><input id="swEnd" class="field" type="date"><button id="swApplyPeriod" class="btn" type="button">Apply</button></div></div><div class="body"><div class="table-wrap"><table class="sales-workspace-table"><thead><tr><th>Nama Sales</th><th>Status</th><th>ID Sales</th><th>Tanggal Bergabung</th><th>Closing</th><th>Komisi</th><th>Pengaturan</th></tr></thead><tbody id="swSalesRows"><tr><td colspan="7" class="sales-workspace-empty">Memuat...</td></tr></tbody></table></div></div></section></section>`;app.appendChild(v);ready=true;bind()}
function setPeriod(k){const n=new Date(),f=d=>{const x=new Date(d);return`${x.getFullYear()}-${String(x.getMonth()+1).padStart(2,'0')}-${String(x.getDate()).padStart(2,'0')}`};if(k==='all')S.start=S.end=null;else if(k==='today')S.start=S.end=f(n);else if(k==='7'){const d=new Date(n);d.setDate(d.getDate()-6);S.start=f(d);S.end=f(n)}else if(k==='30'){const d=new Date(n);d.setDate(d.getDate()-29);S.start=f(d);S.end=f(n)}else{S.start=f(new Date(n.getFullYear(),n.getMonth(),1));S.end=f(n)}$('swStart').value=S.start||'';$('swEnd').value=S.end||'';document.querySelectorAll('#salesWorkspaceView .period-btn').forEach(b=>b.classList.toggle('active',b.dataset.period===k));void load()}
function filtered(){const q=S.search.trim().toLowerCase();return sales.filter(s=>{if(q&&!`${s.name||''} ${s.sales_code||''}`.toLowerCase().includes(q))return false;if(S.status!=='all'&&String(s.status||'').toLowerCase()!==S.status)return false;return true})}
function render(){const rows=filtered();$('swSalesRows').innerHTML=rows.length?rows.map(s=>{const st=String(s.status||'').toLowerCase();return`<tr><td><div class="sales-person">${esc(s.name||'-')}</div></td><td><span class="sales-badge ${st==='active'?'active':'inactive'}">${esc(String(s.status||'-').toUpperCase())}</span></td><td><span class="sales-id">${esc(s.sales_code||'-')}</span></td><td class="sales-date">${esc(fmtDate(s.created_at))}</td><td class="sales-number">${Number(s.sold_codes||0)}</td><td class="sales-money">${money(s.commission)}</td><td><button class="btn sales-action-btn" data-sales-id="${esc(s.id)}" type="button">Kelola</button></td></tr>`}).join(''):'<tr><td colspan="7" class="sales-workspace-empty">Tidak ada sales yang cocok.</td></tr>';$('swSalesRows').querySelectorAll('.sales-action-btn').forEach(b=>b.addEventListener('click',()=>openSalesDetail(b.dataset.salesId)))}
async function load(){if(busy)return;install();if(!ready)return;busy=true;try{const{data,error}=await sb.rpc('admin_sales_workspace',args());if(error)throw error;sales=(data?.sales||[]).map(s=>({id:s.id,sales_code:s.sales_code,name:s.name,status:s.status,created_at:s.created_at,sold_codes:s.sold_codes,commission:s.commission}));render()}catch(e){$('swSalesRows').innerHTML=`<tr><td colspan="7"><div class="notice err">❌ ${esc(msg(e))}</div></td></tr>`}finally{busy=false}}
function openAddSales(){if(creating)return;$('modalTitle').textContent='Add Sales';$('modalBody').innerHTML=`<div class="sales-modal sales-add-modal"><div class="sales-form-grid"><input id="swName" class="field full" placeholder="Nama Sales" autocomplete="off"><input id="swSalesCode" class="field full" placeholder="ID Sales (dibuat admin)" autocomplete="off" maxlength="32"></div><p class="muted">Admin menentukan ID Sales. Tanggal bergabung dibuat otomatis saat data disimpan.</p><div id="swAddMsg"></div><div class="sales-add-actions"><button id="swCreateBtn" class="btn" type="button">Simpan Sales</button></div></div>`;$('modal').classList.add('open');$('swName')?.focus();$('swCreateBtn')?.addEventListener('click',createSales);$('swSalesCode')?.addEventListener('input',e=>{e.target.value=e.target.value.toUpperCase().replace(/[^A-Z0-9_-]/g,'')});$('swSalesCode')?.addEventListener('keydown',e=>{if(e.key==='Enter')createSales()})}
async function createSales(){if(creating)return;const name=$('swName')?.value.trim(),code=$('swSalesCode')?.value.trim().toUpperCase();if(!name){$('swAddMsg').innerHTML='<div class="notice err">Nama sales wajib diisi.</div>';return}if(!code){$('swAddMsg').innerHTML='<div class="notice err">ID Sales wajib diisi.</div>';return}const btn=$('swCreateBtn');creating=true;if(btn)btn.disabled=true;try{const{data,error}=await sb.rpc('admin_create_sales',{p_name:name,p_sales_code:code});if(error)throw error;const created=Array.isArray(data)?data[0]:data;$('swAddMsg').innerHTML=`<div class="notice ok">Sales berhasil dibuat.<br><small>ID Sales: ${esc(created?.sales_code||code)} · Tanggal bergabung: ${esc(fmtDate(created?.created_at))}</small></div>`;await load();setTimeout(()=>$('modal')?.classList.remove('open'),800)}catch(e){$('swAddMsg').innerHTML=`<div class="notice err">❌ ${esc(msg(e))}</div>`}finally{creating=false;if(btn)btn.disabled=false}}
function openSalesDetail(salesId){if(detailBusy)return;$('modalTitle').textContent='Sales Detail';$('modalBody').innerHTML=`<div class="sales-detail-modal"><div id="swDetailBody" class="sales-detail-loading">Memuat detail sales...</div></div>`;$('modal').classList.add('open');void loadSalesDetail(salesId)}
async function loadSalesDetail(salesId){detailBusy=true;try{const{data,error}=await sb.rpc('admin_sales_detail',{p_sales_id:salesId,p_start:S.start?`${S.start}T00:00:00`:null,p_end:S.end?`${S.end}T23:59:59.999`:null});if(error)throw error;const d=data||{},s=d.sales||{},sum=d.summary||{},codes=Array.isArray(d.codes)?d.codes:[],tx=Array.isArray(d.transactions)?d.transactions:[];$('swDetailBody').innerHTML=`<div class="sales-detail-header"><div><div class="sales-detail-title">${esc(s.name||'-')}</div><div class="sales-detail-sub">ID Sales: <strong>${esc(s.sales_code||'-')}</strong> · Bergabung: ${esc(fmtDate(s.created_at))}</div></div><span class="sales-badge ${String(s.status||'').toLowerCase()==='active'?'active':'inactive'}">${esc(String(s.status||'-').toUpperCase())}</span></div><div class="sales-detail-stats"><div class="sales-detail-stat"><span>Kartu Dipegang</span><strong>${Number(sum.codes_held||0)}</strong></div><div class="sales-detail-stat"><span>Closing</span><strong>${Number(sum.sold_codes||0)}</strong></div><div class="sales-detail-stat"><span>Komisi</span><strong>${money(sum.commission)}</strong></div></div><section class="sales-detail-section"><div class="sales-detail-section-head"><div><h3>Kartu yang Dipegang</h3><p class="muted">Masukkan kode kartu untuk mengaitkan kartu ke sales.</p></div></div><div class="sales-assign-row"><input id="swCardCode" class="field" placeholder="Kode kartu"><button id="swAssignBtn" class="btn" type="button">Tambah Kartu</button></div><div id="swAssignMsg"></div><div class="sales-codes-list">${codes.length?codes.map(c=>`<div class="sales-code-item"><div><div class="sales-code-main">${esc(c.card_code||'-')}</div><div class="sales-code-meta">${esc(c.product_type||'-')} · ${esc(String(c.card_status||'-').toUpperCase())} · ${esc(fmtDate(c.assigned_at))}</div></div>${String(c.assignment_status).toLowerCase()==='active'?`<button class="btn sales-unassign-btn" data-assignment-id="${esc(c.assignment_id)}" type="button">Lepas</button>`:''}</div>`).join(''):'<div class="sales-workspace-empty">Belum ada kartu yang dipegang.</div>'}</div></section><section class="sales-detail-section"><div class="sales-detail-section-head"><div><h3>Aktivasi & Komisi</h3><p class="muted">Aktivasi kartu dicatat ke sales yang memegang kartu tersebut saat kartu diaktifkan.</p></div></div><div class="sales-activations-list">${tx.length?tx.map(t=>`<div class="sales-activation-item"><div><div class="sales-code-main">${esc(t.card_code||'-')}</div><div class="sales-code-meta">${esc(fmtDate(t.transaction_date))} · ${esc(t.product_name||'-')} · ${esc(t.customer_name||'-')}</div></div><div class="sales-activation-commission">${money(t.commission)}</div></div>`).join(''):'<div class="sales-workspace-empty">Belum ada aktivasi untuk sales ini.</div>'}</div></section></div>`;$('swAssignBtn')?.addEventListener('click',()=>assignCard(salesId));$('swCardCode')?.addEventListener('keydown',e=>{if(e.key==='Enter')assignCard(salesId)});$('swCardCode')?.addEventListener('input',e=>{e.target.value=e.target.value.toUpperCase().trim()});$('swDetailBody').querySelectorAll('.sales-unassign-btn').forEach(b=>b.addEventListener('click',async()=>{const{error}=await sb.rpc('admin_unassign_sales_card',{p_assignment_id:b.dataset.assignmentId});if(error){$('swAssignMsg').innerHTML=`<div class="notice err">❌ ${esc(msg(error))}</div>`;return}await loadSalesDetail(salesId);await load()}))}catch(e){$('swDetailBody').innerHTML=`<div class="notice err">❌ ${esc(msg(e))}</div>`}finally{detailBusy=false}}
async function assignCard(salesId){const input=$('swCardCode'),code=input?.value.trim().toUpperCase();if(!code){$('swAssignMsg').innerHTML='<div class="notice err">Kode kartu wajib diisi.</div>';return}const btn=$('swAssignBtn');if(btn)btn.disabled=true;try{const{error}=await sb.rpc('admin_assign_sales_card_by_code',{p_sales_id:salesId,p_card_code:code});if(error)throw error;if(input)input.value='';$('swAssignMsg').innerHTML='<div class="notice ok">Kartu berhasil ditambahkan ke sales.</div>';await loadSalesDetail(salesId);await load()}catch(e){$('swAssignMsg').innerHTML=`<div class="notice err">❌ ${esc(msg(e))}</div>`}finally{if(btn)btn.disabled=false}}
function bind(){if($('salesWorkspaceAdd'))$('salesWorkspaceAdd').addEventListener('click',openAddSales);if($('salesWorkspaceRefresh'))$('salesWorkspaceRefresh').addEventListener('click',()=>void load());$('swSearch')?.addEventListener('input',e=>{S.search=e.target.value;render()});$('swStatus')?.addEventListener('change',e=>{S.status=e.target.value;render()});$('swApplyPeriod')?.addEventListener('click',()=>{S.start=$('swStart').value||null;S.end=$('swEnd').value||null;void load()});document.querySelectorAll('#salesWorkspaceView .period-btn').forEach(b=>b.addEventListener('click',()=>setPeriod(b.dataset.period)))}
function show(){install();document.querySelectorAll('.view').forEach(v=>v.classList.toggle('active-view',v.id==='salesWorkspaceView'));$('menuPanel')?.classList.remove('open');$('menuButton')?.setAttribute('aria-expanded','false');history.replaceState?.(null,'','#sales-workspace');window.scrollTo({top:0,behavior:'smooth'});void load()}
window.salesWorkspace=Object.freeze({show,load});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();window.addEventListener('hashchange',()=>{if(location.hash.toLowerCase()==='#sales-workspace')show()});document.addEventListener('aljava:data-loaded',()=>{if(location.hash.toLowerCase()==='#sales-workspace')void load()});})();
